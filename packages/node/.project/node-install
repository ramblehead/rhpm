#!/bin/bash

# For more nvm commands see
# https://github.com/creationix/nvm

set -eu
set -o pipefail

DIR="`dirname \"${BASH_SOURCE[0]}\"`"
if [[ ! -d "$DIR" ]]; then DIR="$PWD"; fi
source "$DIR/configs"
unset DIR

\. "$NVM_DIR/nvm.sh"
nvm install node
