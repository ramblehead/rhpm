#!/bin/bash

set -eu
set -o pipefail

PRJ_PATH="$(dirname "${BASH_SOURCE[0]}")"
if [[ ! -d "$PRJ_PATH" ]]; then PRJ_PATH="$PWD"; fi
PRJ_PATH="$(cd -P "$PRJ_PATH" && pwd)"
source "$PRJ_PATH/paths"
source "$PRJ_PATH/configs"

#export GOBIN=/usr/local/stow/"$PKG_NAME"_"$BLD_NAME"/bin

#if [[ ! -s $SRC_PATH/github.com/ncw/rclone ]]; then
#  mkdir -p $GOBIN
#fi

#echo xxxxxxx $GOPATH

cd $GOSRC/$GITHUB_PATH/$PKG_NAME

#cd $SRC_PATH

go build
